import { StyleSheet, Text, View } from 'react-native'
import React, { useState } from 'react'

const Home = () => {
  const [selectesList,setselectesList]=useState([])
  return (
    <View>
      <Text>Home</Text>
    </View>
  )
}

export default Home

const styles = StyleSheet.create({})
//hook -> tachs logics ra khoi view
// Dem gio hien tai ->hook